# Security Model

## 보호 대상

- Supabase secret/service-role 키와 OAuth provider secrets
- 카카오 계정과 Supabase 사용자 UUID의 연결 정보
- 리뷰·게시물 작성자 UUID
- 기관 등록자 UUID와 사용자의 현재 위치
- 신고자 UUID와 신고 사유
- 원장·운영자·관리자 신뢰 권한
- 권한 변경과 모더레이션 감사 기록

## 신뢰 경계

브라우저, `config.js`, local/session storage, JSON 백업은 모두 신뢰하지 않습니다. 브라우저는 공개 publishable key와 사용자 access token만 가집니다. 모든 최종 권한은 PostgreSQL RLS, 컬럼 GRANT, trigger, `user_roles`로 판정합니다.

Edge Function만 `APP_SUPABASE_SECRET_KEY`를 읽습니다. 이 키는 RLS를 우회할 수 있으므로 역할 변경이라는 단일 목적에만 사용하고, 요청마다 사용자 JWT를 다시 검증한 뒤 DB의 verified admin 여부를 확인합니다.

## 적용된 통제

- 공개 콘텐츠 SELECT에서 `user_id` 컬럼 권한 제거
- 기관 공개 SELECT에서 등록자 UUID 제거, 신규 상태 `pending` 강제
- 기관명·도로명주소 중복, 대한민국 좌표 범위, 시간당 등록 수를 DB에서 재검증
- 등록자 본인은 검수 전 기관만 수정 가능하고 클라이언트는 검수 상태를 지정할 수 없음
- 프로필 본인 행만 조회·수정, 신뢰 역할 컬럼과 사용자 프로필 분리
- 작성자 본인만 UPDATE, 클라이언트 DELETE 미허용
- verified director/admin과 비익명 계정만 채용 작성
- verified operator/admin만 신고 큐 조회와 상태 변경
- 신고 이벤트 직접 SELECT 금지, 공개 RPC는 집계 상태만 반환
- 신고자별 게시물 중복 신고 unique index
- advisory lock 기반 시간당 작성/신고/역할변경 제한
- 서버가 `created_at`, `updated_at`, `handled_by`, `handled_at` 강제 설정
- 역할 변경과 모더레이션 변경의 append-only 감사 로그
- Edge Function의 정확한 CORS allowlist, body 크기/형식 검사, no-store 응답
- publishable key 형식 검사, 세션 저장소 제한, 백업에서 클라우드 자격 증명 제거
- CSP, SRI, clickjacking 차단, 민감 브라우저 기능 차단
- 위치 권한은 현재 origin으로 제한하고 현재 좌표는 앱 상태·백업·교사링크 DB에 미저장
- 브라우저 알림 권한은 사용자가 설정 버튼을 누른 경우에만 요청하며 거부 상태를 우회하지 않음
- 잠금화면 알림에는 기관명·게시물·신고 내용을 넣지 않고 앱 내부 알림센터에서만 상세 내용을 표시
- 알림 클릭 이동 대상은 사전 정의된 앱 화면 허용목록으로 제한해 임의 URL 이동을 차단
- Turnstile 연동 지점과 OAuth redirect origin allowlist
- 리뷰·채용·커뮤니티 자유 본문의 실명, 특정 기관명, 확장 욕설, 명시적 19금 표현, 연락처를 브라우저에서 `**`로 마스킹
- 띄어쓰기·기호 삽입 우회를 정규화해 탐지하되 `시발점`과 피해 신고·성교육 용어는 정상 문맥으로 보존
- 폭력·협박·위해 표현은 등록 차단하고 PostgreSQL trigger가 원문 API 우회 저장을 거부
- 기관 등록명과 리뷰·채용의 구조화된 기관명은 서비스 식별에 필요하므로 별도 검증 후 허용

카카오 지도 JavaScript 키는 브라우저 공개 키이며 비밀키로 취급하지 않습니다. 대신 카카오 개발자 콘솔의 Web 플랫폼 도메인을 localhost와 실제 운영 origin으로 최소화합니다. 주소 검색과 지도 타일 요청은 카카오 서비스로 전송될 수 있으므로 개인정보 처리 안내에 외부 지도 제공자를 명시해야 합니다. 현재 위치는 교사링크 DB에 저장하지 않지만, 지도를 해당 위치로 이동하면 지도 제공자가 요청 지역을 추론할 수 있습니다.

## 익명성 한계

서비스 사용자끼리는 작성자 식별자가 공개되지 않습니다. 그러나 법적 요청, 악용 조사, 계정 복구를 위해 Supabase 프로젝트 관리자와 인증 제공자는 계정과 활동을 연결할 수 있습니다. 따라서 “완전 익명”으로 홍보하면 안 됩니다. 게시물 본문에도 실명, 연락처, 아동 정보, 상세 근무 시점 등 재식별 정보를 입력하지 않도록 정책·신고·운영 절차를 유지해야 합니다.

## 콘텐츠 안전 정책

- 자동 마스킹은 실명·욕설·음란 표현·연락처 노출을 줄이는 보조 수단이며 명예훼손, 모욕, 개인정보 침해 여부를 판정하지 않습니다.
- 정상적인 기관 검색과 리뷰 연결을 위해 전용 기관명 필드에는 공식 기관명을 허용하고, 서술형 본문에서 특정 기관을 직접 지칭하면 마스킹합니다.
- 이름 탐지는 `김민수 원장`, `박 교사`처럼 직함이 붙은 한국어 표현을 우선 처리합니다. 모든 이름을 자동 탐지하면 일반 명사 오탐이 커지므로 신고 검토가 반드시 필요합니다.
- 위협 표현은 마스킹하지 않고 등록을 거부합니다. 아동 이름, 연락처, 주민등록번호 등 고위험 개인정보 신고는 즉시 숨김 후 운영자가 확인하는 절차를 둡니다.
- 운영자는 신고 원문 접근을 최소화하고 숨김·복원 사유를 감사 로그에 남깁니다. 작성자에게 이의제기 경로와 처리 결과를 제공해야 합니다.
- 필터 회피 사례는 원문을 장기 보관하지 말고 탐지 패턴과 익명화된 통계만 개선 자료로 사용합니다.

## 비밀 관리 규칙

- secret/service-role 키를 `config.js`, HTML, 앱 번들, Git, 이슈, 채팅, 로그에 넣지 않습니다.
- 역할마다 별도 Supabase secret key를 만들 수 있으면 Edge Function 전용 키를 사용합니다.
- production과 staging 키 및 프로젝트를 분리합니다.
- 운영자에게 키를 공유하지 않고 개인 카카오 계정에 operator/admin 역할을 부여합니다.
- 관리자 퇴사·분실 시 역할을 revoke하고 활성 세션을 종료합니다.
- 최소 분기마다 미사용 secret key, OAuth redirect, 관리자 목록을 점검합니다.

## 침해 대응

1. 노출된 secret/service-role 키를 즉시 revoke하고 새 키를 발급합니다.
2. Edge Function secret을 교체하고 재배포합니다.
3. `role_audit_log`, `moderation_audit_log`, Supabase Auth/Function 로그에서 노출 시점 이후 활동을 확인합니다.
4. 비정상 `user_roles`를 revoke하고 관련 사용자 세션을 종료합니다.
5. 공개 데이터 변조 여부를 백업과 비교해 복구합니다.
6. 개인정보 영향 범위를 평가하고 필요한 법적 통지 절차를 수행합니다.

## 알려진 운영 과제

- 정적 필터만으로 명예훼손·개인정보·아동 정보 노출을 완전히 차단할 수 없습니다. 사람 기반 신고 검토와 보존/삭제 정책이 필요합니다.
- 신고 사유를 개인정보, 아동정보, 명예훼손·모욕, 위협, 허위 채용, 스팸으로 구조화하고 긴급 신고 우선순위와 이의제기 처리 기한을 정해야 합니다.
- 반복 위반 계정의 작성 간격 제한·정지 정책과 오탐 복원 절차를 운영 약관에 공개해야 합니다.
- 클라이언트 주소 변환은 공식 기관 여부를 증명하지 않습니다. `pending` 기관을 행정 공개 데이터와 대조해 `verified`/`rejected`로 처리하는 운영 절차가 필요합니다.
- 클라이언트 IP 기반 WAF 제한은 이 저장소 범위 밖입니다. 배포 CDN과 Supabase 로그 기반 탐지를 추가해야 합니다.
- 출시 전 한국 개인정보보호·명예훼손·채용 관련 정책을 국내 전문가와 검토해야 합니다.
